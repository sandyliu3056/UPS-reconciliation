# 倉儲計費 — 費率層級

`warehouse_billing.py` 是倉儲計費的桌面工具（tkinter，單一檔案）。這一版把
費率的掛法改成跟 UPS 對帳工具（`index.html` 的 01 Rate Levels → 02 Customers）
同一套：**費率掛在層級上，客戶指到層級**。

```
python warehouse_billing.py
```

## 分層

| | 之前 | 現在 |
| --- | --- | --- |
| 單價表 | 每個客戶各一份 | 每個**層級**一份，客戶指到哪一層就用哪一層 |
| 改一個單價 | 只動那個客戶 | 指到那一層的客戶全部跟著變 |
| 匯入報價表 | 匯進選定的客戶 | 匯進**正在編輯的層級**（工具列那顆） |
| 沒設單價的客戶 | 不會發生（新客戶就帶預設值） | 「未指定」或「層級已刪除」，③ 與 ④ 直接說算不出來 |

分頁順序照著這條相依鏈：

```
① 層級與客戶 ── 左邊層級：一層是一整份獨立的單價表，點一列就是「正在編輯」
                右邊客戶：代碼、名稱、聯絡人、備註，加一欄「費率層級」；
                新增時預設落在正在編輯的那一層
② 費率      ── 改的是正在編輯的層級，標題寫出是哪一層、幾個客戶會跟著變
③ 計算      ── 每個客戶自己的量；單價從它的層級帶入，頁首寫出層級名
④ 帳單      ── 同上；匯出的 xlsx 第二列多一格 Rate Level
```

客戶不在任何共用的下拉選單裡。① ② ③ ④ 每一頁自己有一組「搜尋客戶」框和
清單：打代碼、名稱、聯絡人或層級名就過濾，點一列就把那個客戶帶進這一頁，
四頁的選取互相同步。② 的那一組多做一件事：點到客戶就切到**它的層級**來編
（沒有可用層級的客戶不切，狀態列說原因）。① 的層級清單同樣有搜尋框。

「正在編輯」哪一層擺在工具列，匯入／匯出報價表兩顆按鈕旁邊 —— 那兩顆動的
就是這一層；每一頁都看得到，跟對帳工具右上角的 Editing 一樣。

① 的「新增層級」「複製選定層級」「改名」按下去才跳出小視窗填名字，頁面上
沒有常駐的名稱欄。③ 的兩張卡在寬度夠時並排、不夠時上下疊；每一頁有標題與
說明；所有清單有隔行底色。視窗維持原本大小。

## 規則

- 永遠至少有一層。空檔開起來會自動有一層「Default / 預設」。
- 新層級從報價表預設值起頭，不是一頁空白 —— 第一件事是改這一層不一樣的那幾行。
  要一模一樣的副本用「複製選定層級」。
- **有客戶指到的層級不能刪。** 按下去會列出是哪幾個客戶，去 ② 改派完再刪。
  最後一層也不能刪。
- 客戶指到的層級若已經不在（手動改過 JSON、或別台機器的檔），① 的清單顯示
  `L9（層級已刪除）`而不是「未指定」—— 悄悄顯示成未指定會讓人以為本來就沒設過。
  表單保留那個值，開一次視窗不會被清掉；重新指一層就好。
- 客戶沒有可用的層級時：③ 列出已填的量但單價與金額是「—」，合計是「—」；
  加入作業費、加入項目、匯出帳單都會擋下來並說去哪裡指定。不顯示一個少算了的數字。

## 檔案

`warehouse_billing.json`（與程式同一層，已列入 `.gitignore`）：

```json
{
  "levels":  { "L1": { "name": "預設",  "rates": { "c20": 380.0, "...": null } },
               "L2": { "name": "A 級",  "rates": { "...": 0 } } },
  "clients": { "GEN": { "name": "Geniqua Client", "contact": "", "note": "",
                        "level": "L2", "qty": {}, "amt": {}, "picks": [] } }
}
```

層級 id（`L1`、`L2`…）是內部用的，畫面上只看得到名稱，改名不動 id。

**舊檔會自動搬。** 之前的檔案是每個客戶自帶一份 `rates`。載入時：單價完全一樣的
客戶共用一層；跟報價表預設值一樣的那一層叫「預設」，其餘用客戶代碼命名，
之後在 ① 改名即可。數量、單號都不動。存檔後就是新格式。

## 驗

```
python3 test/test_warehouse_billing.py                 # 資料層，不開視窗
xvfb-run -a python3 test/ui_warehouse_billing.py       # 真的開起來走一遍，每頁截圖
```

第一支只要 openpyxl。第二支要 tkinter 與一個畫面（Xvfb 即可），截圖另需 Pillow。
