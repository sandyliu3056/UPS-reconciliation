{
  "name": "ups-recon-ui-pack",
  "version": "1.0.0",
  "private": true,
  "description": "UPS Reconciliation — UI 補強包（ESC 關窗 / 搜尋延後 / 表頭排序 / 首欄凍結 / 手寫字 / 繪畫感 / 流程圖精修 / 04 分層 / 收掉 Enable-Disable）",
  "scripts": {
    "test": "node test/test-patch.js index.html && node test/test-ink.js index.html && node test/test-contrast.js index.html && node test/test-tabs.js index.html && node test/test-trim.js index.html && node test/test-mascot.js index.html && node test/test-vars.js index.html && node test/test-boot.js index.html && node test/test-track-api.js"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}